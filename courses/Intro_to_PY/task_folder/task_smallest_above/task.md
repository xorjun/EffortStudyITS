# Smallest number above threshold

Create a function that receives a list of numbers and a threshold as input and returns the smallest number in the list above the threshold. If no such number is found, the function should return `None`.

```python
def smallest_above(numbers, threshold)
...

