# Box Plot

**Note:** This task is related to Bonus Task 01.15 from the first exercise sheet.

Assume you have four arrays available:

1. `pre_test_control`
2. `pre_test_intervention`
3. `post_test_control`, and
4. `post_test_intervention`

containing the data recorded during a study, namely 1) the pre-test results in the control group, 2) the pre-test results in the intervention group, 3) the post-test results in the control group, and 4) the post-test results in the intervention group.

## TODO

In a shared axis, create four box plots using the function `matplotlib.pyplot.boxplot`, one for each array. Make sure the box plots and the y-axis are labeled. You also need to use the commant `plt.show()` at the end of your function to make the result visible.