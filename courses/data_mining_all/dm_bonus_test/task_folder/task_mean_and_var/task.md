# Mean and variance

Recall the main descriptive statistics from probability theory: mean and variance.

While they are available as functions in the numpy package, it is important to remember what they refer to.

Without using any other module, implement a function that receives a list of numbers as input and returns the mean of the numbers as first output and the variance as second output.
