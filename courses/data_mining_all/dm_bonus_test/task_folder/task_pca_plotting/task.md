# PCA Plotting

**Note:** This task is related to Bonus Task 02.15 from the second exercise sheet.

Assume you have a data matrix `X` available. Further, the vector `mu` contains the mean of the data and the matrix `V` contains the first two principal components of the data (just how you computed them in the previous task).

## TODO

Now, use `V` and `mu` to reduce `X` to two dimensions, just like principal component analysis (PCA) would do it, and plot the reduced data as points. When you submit your result, the solution plot will show you the result of the `sklearn` implementation of PCA. Is your result consistent with the `sklearn` result? What are differences?
