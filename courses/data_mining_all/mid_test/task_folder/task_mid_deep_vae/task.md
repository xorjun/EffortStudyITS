# Deep Learning: Variational Autoencoders

Assume we have a variational autoencoder and have already computed a matrix `Z` where each row is a sample in the latent space.

Which of the following pieces of code correctly computes the negative log probability of `Z` according to the prior in the latent space?