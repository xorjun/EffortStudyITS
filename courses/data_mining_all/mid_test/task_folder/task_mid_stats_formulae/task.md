# Statistics: Z normalization

Assume an array `X` where entry is a data point. Further, assume that we have already computed the mean `mu = np.mean(X)` and standard deviation `sigma = np.std(X)`. Which piece of code computes the z-normalized version of `X`?