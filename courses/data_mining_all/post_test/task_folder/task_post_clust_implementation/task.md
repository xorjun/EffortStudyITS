# Clustering: K-Means implementation

Assume we have a data matrix `X`, where the data points are rows, and a matrix of means `M`, where the means are rows. Also assume that we have imported the function `scipy.spatial.distance.cdist`.

Which of the following pieces of code correctly implements the E-Step of the K-Means algorithm?