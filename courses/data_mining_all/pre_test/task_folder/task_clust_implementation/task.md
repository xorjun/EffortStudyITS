# Clustering: K-Means implementation

Assume we have a data matrix `X`, where the data points are rows, and an array `k` containing the index of the closest mean for all points.

Which of the following pieces of code correctly implements the M-Step of the K-Means algorithm for the first mean (with index 0)?